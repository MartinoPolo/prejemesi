import Root from './Checkbox.svelte';
import Surface from './CheckboxSurface.svelte';

export { Root, Root as Checkbox, Surface, Surface as CheckboxSurface };
export { checkboxVariants, CHECKBOX_SIZES, type CheckboxSize } from './checkbox_variants.js';
