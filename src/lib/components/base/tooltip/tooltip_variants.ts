import { tv } from 'tailwind-variants';

export const tooltipContentVariants = tv({
	base: 'inline-flex items-center gap-1.5 rounded-btn px-3 py-1.5 text-[11px] font-semibold bg-card text-ink border-2 border-ink shadow-sticker-sm w-fit max-w-xs',
});
