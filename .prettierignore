# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/.local/
/static/
/drizzle/

# Agent tooling. The files under .claude/rules/ are symlinks whose link-target
# text prettier rewrites as markdown (escaping `_` in paths), breaking them on
# every check:all run.
.claude/

# Design mockups — reference-only artifacts, not gated source
/designs/

# Preserve historical artifacts without reformatting them
/archive/**
!/archive/README.md

# Paraglide compiler-owned generated output
/src/lib/paraglide/
