# Archive

Historical material only. Paths mirror their former repository locations; files may describe
rejected designs, obsolete APIs, or unsafe procedures.

Exclude this directory from routine agent searches and implementation context. Consult it only when
historical research is explicitly requested. Supporting assets remain here only to render historical
artifacts; they are not active styling guidance. Use `.mpx/DECISIONS.md` for settled behavior,
`AGENTS.md` for repository rules, and active runbooks for operations. Archived links and commands
are historical, not maintained instructions.
