# Review decision

Refine Variant A with persistent desktop parent menus and adjacent submenus (Windows context-menu behavior), not in-place root replacement. Preserve the accepted mobile Display/bulk sheet hierarchy, hero action positions and gift selection checkbox/card corners.

Use icon-only More and gear-only Settings, with accessible names. Make select-all match gift checkboxes. Keep reorder handles top-left and use the selected small visual inside a larger actionable area. The simplified layout switcher and surrounding app styling are not approved redesigns. Align illustrative menus to actual triggers without claiming to repair the production positioning engine.

The user approved the refined desktop cascades and command direction, and requested finalization after duplicate-text cleanup. That cleanup is complete: the selected count appears once in the sheet header, and each category's current/mixed summary appears once. The scoped command design phase is approved and complete. This decision does not approve the separate gift-geometry variants or production implementation.
